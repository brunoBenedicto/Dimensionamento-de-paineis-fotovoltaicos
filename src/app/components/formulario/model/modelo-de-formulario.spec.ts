import { ModeloDeFormulario } from './modelo-de-formulario';

describe('ModeloDeFormulario', () => {
  it('should create an instance', () => {
    expect(new ModeloDeFormulario()).toBeTruthy();
  });
});
